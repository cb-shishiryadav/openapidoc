---
openapi: post /address/validate
---

import { useState } from 'react';

// The endpoint URL as a string
const endpointUrl = "/api/v2/endpoint/address-validation";

# Address Validation Endpoint

This is the documentation for the Address Validation endpoint.

## Endpoint URL
{endpointUrl}

<button onClick={() => navigator.clipboard.writeText(endpointUrl).then(() => alert('URL copied to clipboard!'))} style={{
  backgroundColor: '#0D9373', 
  color: 'white', 
  border: 'none', 
  padding: '8px 16px', 
  cursor: 'pointer', 
  fontSize: '14px'
}}>
  Copy URL
</button>

### Description
Here you can describe how to use the address validation endpoint.
