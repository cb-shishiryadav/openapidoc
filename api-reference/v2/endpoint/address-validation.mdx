---
openapi: post /address/validate
---

import { Copy } from 'lucide-react'

# POST /address/validate 
<button 
  onClick={() => {
    navigator.clipboard.writeText('/address/validate');
    alert('URL copied!');
  }}
  style={{ 
    marginLeft: '10px',
    background: 'none',
    border: 'none',
    cursor: 'pointer'
  }}
>
  <Copy size={16} />
</button>